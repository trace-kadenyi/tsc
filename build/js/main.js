"use strict";
// Literal types
// unchangeable
let myName;
// strictly only the listed names
let userName;
userName = "Bruce";
// functions
const add = (a, b) => {
    return a + b;
};
// console.log(add(4, 5));
// no return
const logMsg = (message) => {
    console.log(message);
};
logMsg("Hello!");
logMsg(add(2, 3));
//# sourceMappingURL=main.js.map